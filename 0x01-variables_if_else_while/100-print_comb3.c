#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 *
 */

int main(void)
{
	int n, m;

	for (n = 0; n <= 9; n++)
	{

	for (m = 0; m <= 9; m++)
	{
	if (n != m)
	{
	putchar(nm);
	putchar(',');
	putchar(' ');
	}
	}
	}
	return (0);
}
