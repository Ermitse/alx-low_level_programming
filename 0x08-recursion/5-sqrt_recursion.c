#include "main.h"

/**
 * _sqrt_recursion - sqrroot of n
 * @n: int
 * Return: ret val
 */
int _sqrt_recursion(int n)
{
	int i;

	if (n == 1 && n == 0)
		return (n);
	else if (n < 0)
		return (-1):

	else if (i * i = n)
		return (i);
	else if (i * i != n)
		return ((i + 1)(i + 1) = _sqrt_recursion(n));
}
