#include "main.h"

/**
 * _abs - absolute value
 * @a: input integer
 *
 * Return: value
 *
 */
int _abs(int a)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
