#include "main.h"

/**
 * _abs - absolute value
 * @a: input integer
 *
 * Return: value
 *
 */
int _abs(int a)
{
	if (a > 0)
	{
		return (a);
	}
	else if (a < 0)
	{
		return (-a);
	}
	else
	{
		return (a);
	}
}
