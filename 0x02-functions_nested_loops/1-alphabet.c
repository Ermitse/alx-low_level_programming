#include "main.h"

/**
 * print_alphabet - prints alphabets
 *
 * Return: Always 0 (success)
 *
 */

void print_alphabet(void)
{
	for (char lc = 'a' ; lc <= 'z'; lc++)
	_putchar(lc);
	_putchar(lc);
	return (0);
}
