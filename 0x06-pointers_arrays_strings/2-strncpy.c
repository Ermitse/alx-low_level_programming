#include "main.h"

/**
 * _strncpy - string copy
 * @dest: char
 * @src: char
 * @n: int
 * Return: val
 */

char *_strncpy(char *dest, char *src, int n)
{
	int i;

	for (i = 0; src[i] = n; i++)
	{
		dest[] = src[i];
	}
	return (dest);
}
